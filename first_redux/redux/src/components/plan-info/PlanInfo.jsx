import React, { useContext } from 'react';
import InfoChart from '../info-chart/InfoChart';


const PlanInfo = () => {

    return (
        <div>
            <InfoChart />
        </div>
    );
};

export default PlanInfo;